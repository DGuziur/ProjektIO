function cartTest(){
    console.assert(cart != [], "Cart error - is not an empty array")
    console.assert(totalPrice !== 0, "error with total price (not starting at == 0)")
    console.assert(totalItems !== 0, "error with total items (not starting at == 0)")
}